[build]
  publish = "public"
  command = "gatsby build"

[build.environment]
  NODE_VERSION = "16"

# Cache static assets (including images) for 1 year
[[headers]]
  for = "/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable" 